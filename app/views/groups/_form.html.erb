<%= form_for(group) do |f| %>
  <% if group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :object_guid %>
    <%= f.text_field :object_guid %>
  </div>

  <div class="field">
    <%= f.label :displayname %>
    <%= f.text_field :displayname %>
  </div>

  <div class="field">
    <%= f.label :samaccountname %>
    <%= f.text_field :samaccountname %>
  </div>

  <div class="field">
    <%= f.label :mail %>
    <%= f.text_field :mail %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
