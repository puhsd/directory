<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :object_guid %>
    <%= f.text_field :object_guid %>
  </div>

  <div class="field">
    <%= f.label :username %>
    <%= f.text_field :username %>
  </div>

  <div class="field">
    <%= f.label :ldap_imported_at %>
    <%= f.datetime_select :ldap_imported_at %>
  </div>

  <div class="field">
    <%= f.label :ldap_attributes %>
    <%= f.text_field :ldap_attributes %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
